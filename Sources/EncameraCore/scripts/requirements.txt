# Dependencies for all scripts in this directory.
# Build the venv from here:
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
# Then run scripts with .venv/bin/python <script>.py

# Editable App Store Connect client library (used by release.py,
# expire_testflight_builds.py, and app_store_localization/localize.py).
# Brings in PyJWT, cryptography, requests, pyyaml transitively.
-e ./asc

# app_store_localization/localize.py, release.py, expire_testflight_builds.py
openai>=1.0.0
pyyaml>=6.0.1
inquirer>=3.1.3
tabulate>=0.9.0
keyring>=24.2.0
tqdm>=4.66.0

# decrypt.py
pynacl>=1.5.0
rich>=13.0.0
